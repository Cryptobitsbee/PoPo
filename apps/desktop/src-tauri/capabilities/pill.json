{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pill",
  "description": "Minimal permissions for the always-on-top dictation pill.",
  "windows": ["pill"],
  "permissions": [
    "core:event:allow-listen",
    "core:window:allow-set-ignore-cursor-events"
  ]
}
